using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class GlobalVariables
{
    public static TimeSpan bestTimeGlobal = TimeSpan.FromHours(9);
    public static TimeSpan bestTimeGlobalEX = TimeSpan.FromHours(9);

    public static bool EXunlocked = false;
}
